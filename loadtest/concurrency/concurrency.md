Concurrency Testing Summary: Implemented concurrent payment testing to validate financial consistency under simultaneous requests. Identified and resolved race conditions in wallet balance checks and bank-account auto-top-up by using PostgreSQL row-level FOR UPDATE locks within a shared transaction. Verified the flow with 10 concurrent ₹2,000 payments, confirming correct balance updates, minimum bank-balance enforcement, and transactional rollback behavior.